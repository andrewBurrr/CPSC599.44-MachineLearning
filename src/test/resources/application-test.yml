spring:
  profiles:
    active: local

logging:
  config: classpath:logback-test.xml
  #path: classpath:src/test/resources
  pattern:
    level:

configFile: src/main/resources/config.yml
  inputFile: src/main/resources/drivers.csv
  learnedInput: src/main/resources/knownRules.txt
  outputFile: src/main/resources/output.txt
  populationSize: 10
