spring:
  profiles:
    active: local

logging:
  config: classpath:logback.xml
  #path: classpath:log

trainer:
  metrics:
    configFile: src/main/resources/config.yml
    inputFile: src/main/resources/drivers.csv
    outputFile: src/mainresources/output.txt
